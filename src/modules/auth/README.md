Authentication domain module.

- `domain/`: auth rules and invariants
- `application/`: login/session use cases
- `infrastructure/`: persistence/providers
- `ui/`: auth-specific UI pieces if needed
